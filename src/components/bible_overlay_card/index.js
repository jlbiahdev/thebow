$(document).ready(() => {
});

export const cssFilePath = '/src/components/bible_overlay_card/index.css';
export const htmlFilePath = '/src/components/bible_overlay_card/index.html';
export const init = (data) => {
    // $('.cardoverlay img').attr('src', `/src/assets/img/bible/${data.image}`);
}